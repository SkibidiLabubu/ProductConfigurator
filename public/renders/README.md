This directory must contain the render assets used by the configurator preview. The binary files are intentionally
not checked into git to avoid PR tooling errors; before building or deploying, populate this directory with the
render export package (e.g., by downloading from the asset store or copying from `renders/`).
